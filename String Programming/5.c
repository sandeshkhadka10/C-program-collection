//program to print the ASCII value of the given character
#include<stdio.h>
int main()
{
	char temp;
	printf("Enter the character: ");
	scanf("%c",&temp);
	printf("The ASCII value of %c is %d.",temp,temp);
	return 0;
}
